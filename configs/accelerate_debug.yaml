# 单卡调试配置：无 DeepSpeed，标准 PyTorch，启动最快
# 用法: accelerate launch --config_file configs/accelerate_debug.yaml -m training.train_llama
compute_environment: LOCAL_MACHINE
distributed_type: 'NO'
mixed_precision: 'no'
num_machines: 1
num_processes: 1
use_cpu: false
